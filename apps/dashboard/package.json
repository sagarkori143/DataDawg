{
  "name": "@ollive/dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The observability UI. Runs on its own host, reads only telemetry, never touches the chat path.",
  "scripts": {
    "dev": "next dev -p 3002",
    "build": "next build",
    "start": "next start -p 3002",
    "vercel-build": "npm --prefix ../.. run build:packages:dashboard && next build"
  },
  "dependencies": {
    "@ollive/config": "*",
    "@ollive/db": "*",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "recharts": "^3.7.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2"
  }
}
